# リリースノート 2026-06-22

**Agent 9: ReleaseManager**

## 概要
CAメンバーに **福田・清野** を追加（4名→6名）。あわせて月またぎ時の週次KPI実績リセット、Vercel自動デプロイ連携の復旧を実施。

## 変更内容
- CAメンバー追加: `CA_NAMES` に福田・清野を追加（単一定義から全UI自動追従）
- API補完ロジックを `CA_NAMES.length` 参照化（4名ハードコード解消・後方互換）
- セキュリティ是正: `/api/data` GET の `select('*')` → `select('week_key,payload')`
- （関連）月またぎ新規週で週次KPI実績8項目を0リセット
- （関連）`vercel git connect` でGitHub↔Vercel自動デプロイを再接続

## デプロイ前チェックリスト
- [x] `npm run build` 成功
- [x] TypeScript型エラー 0件（`tsc --noEmit`）
- [x] ESLint（next build内Lint）警告 0件
- [x] 環境変数の追加・変更なし
- [x] DBマイグレーション不要（JSON payload方式・スキーマ不変）
- [x] SecurityReview / PerformanceReview / IntegrationTest 全PASS

## デプロイ
1. DBマイグレーション: 不要
2. `git push origin main` → Vercel自動デプロイ（連携復旧済み）
3. スモークテスト: 本番HTMLに6名表示を実測 ✅
4. 本リリースノート記録 ✅

## ロールバック手順
- 影響は後方互換のため、`CA_NAMES` から2名を除去して再デプロイで旧構成に戻せる（payloadは6要素のまま残るが余剰インデックスは未参照で無害）。

## 関連ドキュメント
spec.md / architecture.md / data-design.md / design.md / security-review.md / performance-review.md / integration-test-2026-06-22.md
